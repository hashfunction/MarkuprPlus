# electron-builder.yml
# Desktop Distribution Configuration for MarkuprPlus
# Version: 0.4.0

appId: com.eddiesanjuan.markuprx
productName: MarkuprPlus
executableName: MarkuprPlus
copyright: Copyright 2026 Eddie San Juan

directories:
  output: release
  buildResources: build

# Explicit null prevents electron-builder from inferring a GitHub updater feed
# from the local git remote and embedding app-update.yml in packaged apps.
publish: null

# Rebuild native modules for Electron's Node.js version
npmRebuild: true

files:
  - dist/**/*
  - package.json

extraResources:
  - from: assets
    to: assets
    filter:
      - "tray-{idle,recording,complete,error}{,Template,@2x,Template@2x}.png"
      - "tray-processing{,-0,-1,-2,-3}{,Template,@2x,Template@2x}.png"

# whisper.cpp is executed as a child process and its Metal shader is loaded at
# runtime, so both files must live outside the read-only app.asar archive.
asarUnpack:
  # Native addons must retain their on-disk relative paths. In particular,
  # Sharp resolves libvips beside its .node binary and cannot run from ASAR's
  # temporary dlopen extraction directory.
  - "node_modules/**/*.node"
  - "node_modules/whisper-node/lib/whisper.cpp/main*"
  - "node_modules/whisper-node/lib/whisper.cpp/ggml-metal.metal"

beforePack: scripts/prepare-whisper-runtime.cjs

# =============================================================================
# macOS Configuration
# =============================================================================
mac:
  category: public.app-category.productivity
  icon: build/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false
  # scripts/notarize.cjs is the single notarization path. Disabling
  # electron-builder's built-in pass prevents it from consuming the same
  # credentials before the afterSign hook runs.
  notarize: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.inherit.plist
  # Hide from dock and Cmd+Tab - pure menu bar app
  extendInfo:
    LSUIElement: true
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64

# =============================================================================
# DMG Installer Configuration
# =============================================================================
dmg:
  background: build/dmg-background.png
  iconSize: 100
  iconTextSize: 12
  window:
    width: 660
    height: 400
  contents:
    - x: 180
      y: 170
      type: file
    - x: 480
      y: 170
      type: link
      path: /Applications
  # electron-builder expands ${arch} to "-arm64" or an empty string on macOS.
  title: "Install MarkuprPlus${arch}"
  artifactName: "markuprplus-${version}-${arch}.dmg"

# =============================================================================
# Windows Configuration
# =============================================================================
win:
  icon: build/icon.ico
  # Windows taskbar icons (overlay and thumbnail toolbar)
  extraResources:
    - from: build
      to: build
      filter:
        - "overlay-*.png"
        - "toolbar-*.png"
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
  # Publisher identity is derived from the signing certificate common name.
  # Signed release validation must inspect the actual Authenticode certificate subject.
  verifyUpdateCodeSignature: false
  # File associations
  fileAssociations:
    - ext: markuprx
      name: MarkuprPlus Session
      description: MarkuprPlus feedback session file
      icon: build/icon.ico
      role: Editor

nsis:
  oneClick: false
  perMachine: true
  allowElevation: true
  allowToChangeInstallationDirectory: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeader: build/installer-header.bmp
  installerSidebar: build/installer-sidebar.bmp
  license: LICENSE
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "MarkuprPlus"
  artifactName: "markuprplus-Setup-${version}.exe"
  include: build/installer.nsh
  deleteAppDataOnUninstall: false

# =============================================================================
# Linux Configuration (for future use)
# =============================================================================
linux:
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  category: Development
  icon: build/icon.png
  artifactName: "markuprplus-${version}-${arch}.${ext}"

# =============================================================================
# Code Signing & Notarization
# =============================================================================
afterSign: scripts/notarize.cjs
