# MarkuprPlus MCP session example

This fictional report illustrates a `capture_with_voice` result from the source-built MCP server. Evidence paths are code-formatted references rather than embedded missing files.

## Grid layout breaks at the medium breakpoint

- **Time:** 00:04
**Evidence:** `frames/frame-0004.png`

At 768 px, the third product card wraps to another row. The combined container padding and grid gap leave less width than the three declared tracks require.

## Filter controls reproduce the same constraint

- **Time:** 00:41–00:52
**Evidence:** `frames/frame-0041.png`, `frames/frame-0052.png`

The category controls wrap at the same breakpoint, suggesting the shared container width/gap contract is the root cause rather than card content.

## Proposed verification

- **Time:** 01:21
**Evidence:** `frames/frame-0081.png`

Add viewport assertions immediately below, at, and above 768 px. Verify grid track count, no horizontal document overflow, and keyboard access to every filter control.

---

*Generated by [MarkuprPlus](https://markuprplus.com)*
